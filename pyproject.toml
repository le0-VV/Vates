[project]
name = "mlx-streaming-moe"
version = "0.1.0"
description = "Apple Silicon 上的显存外置流式 MoE 推理 + Qwen3-Next MTP 自投机解码(基于 MLX)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mlx>=0.31",
    "mlx-lm>=0.31",
    "mlx-vlm==0.3.12",
    "numpy>=2.0",
    "Pillow>=10.3.0",
    "requests>=2.31.0",
    "textual>=0.80",
]

# 命令行入口:vates chat 交互式对话(MTP 自投机快路径)
[project.scripts]
vates = "mlx_streaming.cli:main"

[dependency-groups]
dev = [
    "cmake>=4.0",
    "nanobind>=2.12.0",
    "pytest>=8",
    "pytest-asyncio>=0.23",
]

# 默认用清华镜像(pypi 在国内慢/TLS 偶发重置)。如不需要可删除本段。
[[tool.uv.index]]
url = "https://pypi.tuna.tsinghua.edu.cn/simple"
default = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# 包名仍为 mlx_streaming(保持现有 import 路径不变)
[tool.hatch.build.targets.wheel]
packages = ["mlx_streaming"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["mlx_streaming/tests"]
asyncio_mode = "auto"
